interface Routes {
  LOGIN: string;
  REGISTER: string;
  HOME: string;
  PROFILE: string;
}

const routes: Routes = {
  // Auth
  LOGIN: 'Login',
  REGISTER: 'Register',

  // Main
  HOME: 'Home',
  PROFILE: 'Profile',
};

export default routes;
